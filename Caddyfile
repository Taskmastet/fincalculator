# Локальный HTTPS-сервер для калькулятора "В час".
# Caddy сам выпускает доверенный самоподписанный сертификат для localhost.
# Запуск:  caddy run --config Caddyfile
https://localhost {
	root * .
	file_server
	encode gzip
}
